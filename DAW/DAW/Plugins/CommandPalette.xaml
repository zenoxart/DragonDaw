<Window x:Class="DAW.Plugins.CommandPalette"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Command Palette"
        Width="550" Height="400"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        WindowStartupLocation="CenterOwner">
    
    <Window.Resources>
        <Style x:Key="SearchBox" TargetType="TextBox">
            <Setter Property="Background" Value="#21262D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="CaretBrush" Value="#5BA4E6"/>
        </Style>
        
        <Style x:Key="ResultItem" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#26619C"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#21262D"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Border Background="#161B22" BorderBrush="#26619C" BorderThickness="1" CornerRadius="12">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="10" Opacity="0.6" Color="Black"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Search Header -->
            <Border Grid.Row="0" Background="#1A4570" CornerRadius="12,12,0,0" Padding="16,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="🔌" FontSize="18" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="Plugin Browser" FontWeight="Bold" Foreground="White" FontSize="14"/>
                        <TextBlock Text="Type to search effects and plugins" Foreground="#8899AA" FontSize="10"/>
                    </StackPanel>
                    <TextBlock Grid.Column="2" Text="ESC to close" Foreground="#667788" FontSize="10" VerticalAlignment="Center"/>
                </Grid>
            </Border>
            
            <!-- Search Input -->
            <Border Grid.Row="1" Background="#0D1117" Padding="4">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="🔍" FontSize="14" 
                               VerticalAlignment="Center" Margin="12,0,0,0" Opacity="0.5"/>
                    <TextBox x:Name="SearchBox" Grid.Column="1" Style="{StaticResource SearchBox}"
                             TextChanged="SearchBox_TextChanged"
                             PreviewKeyDown="SearchBox_PreviewKeyDown"/>
                </Grid>
            </Border>
            
            <!-- Results -->
            <ListBox x:Name="ResultsList" Grid.Row="2" 
                     Background="Transparent" BorderThickness="0"
                     ItemContainerStyle="{StaticResource ResultItem}"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     MouseDoubleClick="ResultsList_MouseDoubleClick"
                     PreviewKeyDown="ResultsList_PreviewKeyDown">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Icon -->
                            <Border Grid.Column="0" Background="#21262D" CornerRadius="6" 
                                    Width="32" Height="32" HorizontalAlignment="Left">
                                <TextBlock Text="{Binding Icon}" FontSize="16" 
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            
                            <!-- Name and Description -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="8,0,0,0">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13"/>
                                <TextBlock Text="{Binding Description}" FontSize="10" 
                                           Foreground="#888888" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                            
                            <!-- Category Badge -->
                            <Border Grid.Column="2" Background="#26619C" CornerRadius="4" 
                                    Padding="8,3" VerticalAlignment="Center" Margin="8,0">
                                <TextBlock Text="{Binding Category}" FontSize="9" Foreground="White"/>
                            </Border>
                            
                            <!-- Usage indicator -->
                            <TextBlock Grid.Column="3" Text="{Binding UsageCount, StringFormat='×{0}'}" 
                                       FontSize="9" Foreground="#555555" VerticalAlignment="Center"
                                       Visibility="{Binding UsageCount, Converter={StaticResource CountToVis}}"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            
            <!-- Footer -->
            <Border Grid.Row="3" Background="#0D1117" CornerRadius="0,0,12,12" Padding="12,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <TextBlock Text="↑↓" Foreground="#555" FontSize="10" Margin="0,0,4,0"/>
                        <TextBlock Text="Navigate" Foreground="#666" FontSize="10" Margin="0,0,16,0"/>
                        <TextBlock Text="↵" Foreground="#555" FontSize="10" Margin="0,0,4,0"/>
                        <TextBlock Text="Open Plugin" Foreground="#666" FontSize="10"/>
                    </StackPanel>
                    
                    <TextBlock Grid.Column="1" x:Name="ResultCount" 
                               Text="5 plugins" Foreground="#555" FontSize="10"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
